sudo: false
language: ruby
rvm:
  - 2.3.4

services:
  - mysql
  - redis-server

cache:
  - bundler

before_install:
  - bundle install
  - gem install dotenv

script:
  - bundle exec rake
  - cd tests
  - export BUNDLE_GEMFILE=Gemfile
  - bundle install
  - bundle exec cucumber